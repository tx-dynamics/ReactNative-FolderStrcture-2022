import { colors } from "../utilities";

const theme = {
    light: {
        theme: 'light',
        color: colors.black,
        background: colors.white,

    },
    dark: {
        theme: 'dark',
        color: colors.white,
        background: colors.black,
    },
}

export default theme